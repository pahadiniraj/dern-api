{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" },
    { "src": "prisma/schema.prisma", "use": "@vercel/static-build", "config": { "buildCommand": "prisma generate" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
